 ALGORITO DEL PROYECTO

1 Leer instrucciones 
2 Descarge el projecto.JS
3 Abrir el projecto.js con NotePad++
4 Leer el codigo 
5 analizar detalladamente el codigo 
6 volver a leer las instrucciones
7 volver a projecto
8 ir a la instruccion 1del projecto
9 leer las intrucciones 
10 Ir a la instruccion 1 del projecto  "1. convierta la cantidad de hijos a numero"
11 Crear una variable en llamada "numerodehijos"
12 Crear otra variable en  llamada "tienehijos"
13 preguntar con el comando prompt al usuario si tiene  hijos
14 Crear un if 
15 comparar si el texto "SI" es igual a la variable "tienehijos"
16 agregar el metodo toUpperCase a la variable "tiene"hijos" para que ignore mayusculas
16.1 Si la condicion es "SI" entonces usar la variable "numeroshijos" 
16.2 usando la variable "numerohijos" y prompt, pedir cuantos hijos de 1 a 5
16.3 crear una variable "cantidad_hijos"
16.4 en la variable "cantidad_hijos" colocar el comando = parseInt para poder pasar la variable tipo texto "numerohijos" a numero
17 volver al leer las instrucciones 
18 volver al projecto
19 ir a la instruccion 2 del projecto 
19 leer las intrucciones 
20 Hacer la seguna instruccion "2. Recargo por la edad del conyuge"
21 crear un if 
22 agregamos una condicion usando la variable "edad_conyuge_numero" en la que vemos si es mayor igual que 18
23 agregamos && para agregar otra condicion
24 agregamos la condicion si la variable "edad_conyuge_numero" es menor a 25
25 dentro del if ponemos la variable "recargo"
26 colocamos a la par de la variable "recargo" que es igual a las variables "precio base" por "casado 18"
27 en la siguiente linea colocamos la variable "recargo_total"
28 colocamos en la varaible "recargo_total" que es igual a "recargo_total" mas "recargo"
29 crear un  else if 
30 agregamos una condicion usando la variable "edad_conyuge_numero" en la que vemos si es mayor igual que 25
31 agregamos && para agregar otra condicion
32 agregamos la condicion si la variable "edad_conyuge_numero" es menor a 50
33 dentro del if else ponemos la variable "recargo"
34 colocamos a la par de la variable "recargo" que es igual a las variables "precio base" por "casado 25"
35 en la siguiente linea colocamos la variable "recargo_total"
36 colocamos en la varaible "recargo_total" que es igual a "recargo_total" mas "recargo"
37 crear un  else if 
38 agregamos una condicion usando la variable "edad_conyuge_numero" en la que vemos si es mayor igual que 50
39 dentro del if else ponemos la variable "recargo"
40 colocamos a la par de la variable "recargo" que es igual a las variables "precio base" por "casado 50"
41 en la siguiente linea colocamos la variable "recargo_total"
42 colocamos en la varaible "recargo_total" que es igual a "recargo_total" mas "recargo"
43 volvemos a leer instrucciones
44 Volvemos al projecto
45 vamos a las instrucciones 3 del projecto "3. Recargo por la cantidad de hijos"
46 creamos un if 
47 agregamos una condicion en la que la variable "cantidad_hijos" tiene que ser igual 1
48 dentro del if ponemos la variable "recargo"
49 colocamos a la par de la variable "recargo" que es igual a las variables "precio base" por "hijos"
50 en la siguiente linea colocamos la variable "recargo_total" 
51 colocamos a la par de la variable "recargo_total" que es igual a las variables "recargo total" por "recargo"
52 creamos un else if
53 agregamos una condicion en la que la variable "cantidad_hijos" tiene que ser igual 2
54 cambiamos el valor de la varaible "hijos" a 0.4
55 dentro del else if  ponemos la variable "recargo"
56 colocamos a la par de la variable "recargo" que es igual a las variables "precio base" por "hijos"
57 en la siguiente linea colocamos la variable "recargo_total" 
58 colocamos a la par de la variable "recargo_total" que es igual a las variables "recargo total" por "recargo
59 creamos un else if
60 agregamos una condicion en la que la variable "cantidad_hijos" tiene que ser igual 3
61 cambiamos el valor de la varaible "hijos" a 0.6
63 dentro del else if  ponemos la variable "recargo"
64 colocamos a la par de la variable "recargo" que es igual a las variables "precio base" por "hijos"
65 en la siguiente linea colocamos la variable "recargo_total" 
66 colocamos a la par de la variable "recargo_total" que es igual a las variables "recargo total" por "recargo
67 creamos un else if
68 agregamos una condicion en la que la variable 8cantidad_hijos" tiene que ser igual 4
69 cambiamos el valor de la varaible "hijos" a 0.6
70 dentro del else if  ponemos la variable "recargo"
71 colocamos a la par de la variable "recargo" que es igual a las variables "precio base" por "hijos"
72 en la siguiente linea colocamos la variable "recargo_total" 
73 colocamos a la par de la variable "recargo_total" que es igual a las variables "recargo total" por "recargo
74 creamos un else if
75 agregamos una condicion en la que la variable "cantidad_hijos" tiene que ser igual 5
76 cambiamos el valor de la varaible "hijos" a 1.0
77 dentro del else if  ponemos la variable "recargo"
78 colocamos a la par de la variable "recargo" que es igual a las variables "precio base" por "hijos"
79 en la siguiente linea colocamos la variable "recargo_total"  
80 colocamos a la par de la variable "recargo_total" que es igual a las variables "recargo total" por "recargo
81 colocamos un else
82 dentro del else ponemos una alerta con "alert"
82 dentro de la alerta ponemos "Numero de hijos invalido"
83 Guardar cambios 
84 cerrar Notepad++
85 cerrar Notepad
86 crear una carpeta y nombrarla projecto JS
87 poner el archivo del algoritmo creado en la carpeta projecto JS
88 poner el archivo .JS en la carpeta de projecto JS
89 crear un archivo llamado README.MD dentro de la carpeta
90 abrir el archivo README.MD
91 Escribir un resumen del projecto
92 cerrar el archivo README.MD
93 dentro de la carpeta click derecho 
94 click izquiero en "GIT BASH"
95 dentro de git poner el comando "git init"
96 dentro de git poner el comando "git commit -am "projecto JS"
97 abrir un navegador
98 entrar a github
99 crear un repositorio 
100 copiar el link para subir los archivos al repositorio
101 poner el comando en GIT " git remote add origin https://github.com/MegariderVACations/Proyecto-JS.git"
102 poner el comando en git "git push origin master"
103 cerrar GIT
104 celebrar porque este fue muy largo :D

